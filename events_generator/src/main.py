# dicts with user_id and product_id
# a method to every action (login, click, purchase) with user_id and product_id variables
# and a method to generate a random event with a random user_id and product_id